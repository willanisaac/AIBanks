# Copia este archivo como .env (NO lo commitees) y completa valores.

# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Backend IA (FastAPI) usado por MatchCard (/predict) y useTier (/predict_segmento)
VITE_AI_BACKEND_URL=http://127.0.0.1:8000

# Gemini (LLM) — se consume DIRECTO desde el navegador
# Importante: al estar en frontend, restringe la API key por dominio/origen en Google Cloud.
VITE_GEMINI_API_KEY=AIzaSyBvq-cev9BXqsiu6kla4JV7XcA7hhPQj6s 
VITE_GEMINI_MODEL=gemini-1.5-flash
